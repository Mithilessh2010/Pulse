import DemoPageContent from "@/components/DemoPageContent";

export const metadata = {
  title: "Demo — Pulse",
  description: "Walk through how Pulse helps teams spot blockers, approve work, track workload, and understand project health.",
};

export default function DemoPage() {
  return <DemoPageContent />;
}
