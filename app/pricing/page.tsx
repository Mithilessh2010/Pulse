import PricingPageContent from "@/components/PricingPageContent";

export const metadata = {
  title: "Pricing — Pulse",
  description:
    "Simple Pulse pricing for teams replacing scattered tools with one operating system for work, approvals, spend, and visibility.",
};

export default function PricingPage() {
  return <PricingPageContent />;
}
